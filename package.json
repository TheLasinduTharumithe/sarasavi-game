{
  "name": "sarasavi-catch-books",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "lint": "tsc --noEmit",
    "test": "node --test src/game/logic/*.test.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "firebase:deploy": "npx firebase-tools deploy --only firestore:rules,firestore:indexes --project sarasavigame"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.3.0",
    "vite-plugin-pwa": "^1.3.0"
  },
  "dependencies": {
    "firebase": "^12.19.0",
    "phaser": "^3.90.0"
  }
}
